- rule: Container running as root
  desc: Detects a container running as root
  condition: container and user.uid = 0
  output: "Container running as root (user=%user.name command=%proc.cmdline)"
  priority: WARNING